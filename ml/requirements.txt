# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-multipart>=0.0.12
httpx>=0.27

# --- Document parsing (M3) ---
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0

# --- Chunking (M3) ---
langchain-text-splitters>=0.3

# --- Embeddings (M3). torch is installed CPU-only in the Dockerfile. ---
sentence-transformers>=3.3
transformers>=4.46

# --- Vietnamese word segmentation, pure Python, no JVM (M3) ---
underthesea>=6.8

# --- Qdrant client: read-only smoke/verification only; .NET owns all writes ---
qdrant-client>=1.12

# --- Evaluation / RAGAS (M5) ---
ragas>=0.2
datasets>=3.0
openai>=1.54
langchain-ollama>=0.2
langchain-openai>=0.2
